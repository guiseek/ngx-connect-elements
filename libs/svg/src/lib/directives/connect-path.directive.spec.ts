import { ConnectPathDirective } from './connect-path.directive';

describe('ConnectPathDirective', () => {
  it('should create an instance', () => {
    const directive = new ConnectPathDirective();
    expect(directive).toBeTruthy();
  });
});
